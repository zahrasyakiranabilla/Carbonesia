info:
  name: 01 List Users
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/v1/admin/users"

runtime:
  variables:
    - name: baseUrl
      value: http://localhost:8080

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
