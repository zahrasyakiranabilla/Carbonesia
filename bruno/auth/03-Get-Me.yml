info:
  name: 03 Get Me
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/auth/me"
  headers:
    Authorization: "Bearer {{accessToken}}"
    Content-Type: application/json

settings:
  encodeUrl: true