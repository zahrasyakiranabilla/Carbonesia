info:
  name: 05 Forgot Password
  type: http
  seq: 5

http:
  method: POST
  url: "{{baseUrl}}/api/auth/forgot-password"
  body:
    type: json
    data: |-
      {
        "email": "{{email}}"
      }
  headers:
    Content-Type: application/json

settings:
  encodeUrl: true
